{
  "name": "Parachute: Timer",
  "description": "Listens for the launched event (typically emitted from detect-launch) and then deploys the parachute after a set amount of time.",
  "parameters": {
    "delay": {
      "description": "Number of milliseconds to wait after launch before deploying",
      "type": "number",
      "default": 1500
    }
  }
}
